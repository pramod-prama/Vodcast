# F5-TTS Client API Requirements
# Dependencies for using F5-TTS in your projects

# HTTP requests
requests>=2.28.0

# JSON handling (built-in)
# json - built-in Python module

# File operations (built-in)
# os, pathlib - built-in Python modules

# Type hints (built-in in Python 3.5+)
# typing - built-in Python module

# Base64 encoding (built-in)
# base64 - built-in Python module

# Optional: For web frameworks
Flask>=2.3.0
Django>=4.0.0
FastAPI>=0.100.0
uvicorn>=0.20.0

# Optional: For mobile development
# React Native / Flutter dependencies would be platform-specific

# Optional: For async operations
aiohttp>=3.8.0
asyncio  # built-in in Python 3.4+

# Optional: For audio processing
pydub>=0.25.0
soundfile>=0.12.0
